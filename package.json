{
  "name": "nexus",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev --filter=@nexus/web",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "rimraf node_modules .turbo"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "turbo": "^2.2.3"
  },
  "dependencies": {
    "lucide-react": "^0.552.0"
  }
}
